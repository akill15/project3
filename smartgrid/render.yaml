services:
  - type: web
    name: project2
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir backend app_smartgrid:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
